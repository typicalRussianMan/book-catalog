import React from "react";

/**
 * the screen that is displayed while the content is loading
 */

export class Loading extends React.Component {
    render() {
        return (
            <div className="loading">Loading...</div>
        )
    }
}